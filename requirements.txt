# Pulsar v1.0 — Requirements
# Instalar en entorno virtual: pip install -r requirements.txt
#
# Python: usar 3.11 o 3.12. Con 3.14 algunas dependencias transitivas
# (p. ej. pyiceberg/pyroaring) no tienen wheels y exigen compilación C++
# (Microsoft Visual C++ Build Tools en Windows).

# Core
streamlit>=1.43.0
# Fijar <2.24 para evitar storage3 con pyiceberg/pyroaring (requieren C++ en Windows).
supabase>=2.10.0,<2.24

# Data
pandas>=2.2.0,<3
# Fijar 5.4.1: 5.5+ usa TypedDict(closed=...) incompatible con Python 3.14.
altair==5.4.1

# Payments
stripe>=11.4.0

# PDF
reportlab>=4.2.0

# Excel
openpyxl>=3.1.0

# Email
sendgrid>=6.11.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Agents
anthropic>=0.49.0

# Resiliencia (retry en Supabase)
tenacity>=8.1.0

# Tests
pytest>=7.0.0
